## Autoreloading project
To reload the website every time you make changes to the source code, follow the following steps:-

1. Install NPM packages into the project root
```
npm install
```

2. Run BrowserSync with Laravel Mix
```
npm run watch
```